# 📂 Ignorer les fichiers personnels ou sensibles

app.py                  # Prototype blanc local non déployé

# 🔐 Clés et configurations locales
.env                    # Variables d’environnement
*.env

# 🧠 Données mémoire (optionnel, sauf si tu veux les versionner)
memoire/*.json          # Mémoires privées (si tu veux les exclure)

# 🧪 Fichiers Python compilés
__pycache__/
*.py[cod]

# 📦 Dépendances virtuelles
venv/
.env/
env/
.venv/

# 🧬 Jupyter Notebooks ou fichiers temporaires
*.ipynb
*.log
*.bak
*.swp
*.tmp

# 💻 IDE & système
.DS_Store               # macOS
Thumbs.db               # Windows
*.~                    # fichiers temporaires d’éditeurs
.vscode/
.idea/
git rm --cached app.py
git commit -m "Ignorer app.py (prototype blanc personnel)"

